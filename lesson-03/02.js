// Разработайте функцию isEven, которая определяет, чётное ли число.
// Функция должна возвращать true, если число чётное, и false — в противном случае
clear ==true;
odd == false;
function isEven(clear, odd) {
   if (clear) {
    console.log(четное);
   } if (odd ==false) {
    console.log(нечетное);
   }
}
isEven(clear,odd)
